{
  "name": "@1eg/cert-manager-to-azion",
  "version": "0.1.0",
  "description": "Copy cert-manager certificate to Azion",
  "index": "./lib/index.js",
  "bin": {
    "cert-manager-to-azion": "./lib/index.js"
  },
  "scripts": {
    "clean": "rimraf ./lib",
    "build": "npm run clean && tsc",
    "dev": "ts-node ./src/index.ts"
  },
  "license": "ISC",
  "dependencies": {
    "@kubernetes/client-node": "^0.14.0",
    "axios": "^0.21.1",
    "chalk": "^4.1.0",
    "commander": "^7.1.0"
  },
  "devDependencies": {
    "@types/node": "^10.17.54",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.2",
    "rimraf": "^3.0.2"
  },
  "files": [
    "lib/**/*"
  ],
  "engines": {
    "node": ">= 10.17"
  }
}
